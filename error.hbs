{{!< default}}

<main class="site-main">
    <header class="single-header gh-canvas">
        <div class="single-meta">{{statusCode}}</div>
        <h1 class="single-title">{{message}}</h1>
        <p class="single-excerpt"><a href="{{@site.url}}">{{@custom.back_link}}</a></p>
    </header>
</main>

{{!< default}}

<main class="site-main">

    {{#is "paged"}}{{else}}
    <header class="feed-header gh-canvas">
        {{> "subscribe"}}
    </header>
    {{/is}}

    <div class="post-feed gh-feed gh-canvas">
        {{#foreach posts}}
            {{> "loop"}}
        {{/foreach}}
    </div>

    {{> "pages-nav"}}

</main>
